name: Build Android APK for Release

on:
  push:
    tags:
      - 'v*' # Triggers on any tag starting with 'v', e.g., v1.0.0

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install Dependencies
        working-directory: ./client-app/client
        run: npm ci

      - name: Build Web Assets
        working-directory: ./client-app/client
        run: npm run build

      - name: Capacitor Sync
        working-directory: ./client-app/client
        run: npx cap sync android

      - name: Build Android APK
        working-directory: ./client-app/client/android
        run: chmod +x gradlew && ./gradlew assembleDebug

      - name: Rename APK
        run: mv client-app/client/android/app/build/outputs/apk/debug/app-debug.apk Mithra-Life-OS-${{ github.ref_name }}.apk

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: Mithra-Life-OS-${{ github.ref_name }}.apk
          name: Mithra Life OS Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
